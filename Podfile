source 'https://github.com/CocoaPods/Specs.git'
source 'git@github.com:urbn/URBNPrivatePodSpec.git'

# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'SnapAndMatch' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for SnapAndMatch
  pod 'SwiftyJSON', '~> 3.1'
  pod 'URBNSwiftyModels'
  pod 'URBNConvenience'
  pod 'Kingfisher', '~> 3.0'

  target 'SnapAndMatchTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'SnapAndMatchUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
